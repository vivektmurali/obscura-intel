# Requirements (synthesized from PRD-classified sources)

**No PRD-type documents were present in this ingest batch.**

Classifications consumed (`CLASSIFICATIONS_DIR`): `ARCHITECTURE-a3f91c22.json` (SPEC),
`DECISIONS-9845416c.json` (DOC), `HANDOVER-d56233fc.json` (ADR),
`PARKING-000549bd.json` (DOC), `PREREGISTRATION-000018a4.json` (ADR).

None classified as `PRD`. This file is intentionally empty of requirement entries.
If a future ingest batch adds a PRD-type document, re-run synthesis to populate
`REQ-{slug}` entries here — do not hand-author requirements into this file, since
its provenance guarantee (`source: {path}` per entry) depends on it being
synthesizer-generated.
