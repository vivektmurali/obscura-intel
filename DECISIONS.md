# Decisions log

Running log of autonomous implementation decisions per CLAUDE.md rule 5.

## Phase 0

- **GDELT rate limiting is aggressive from this network.** The 5s inter-call sleep in the HANDOVER.md template was insufficient — hit 429 repeatedly, once requiring ~4 min of cumulative backoff before succeeding. Changed `scripts/00_smoke_gdelt.py` to: 20s sleep between calls, retry loop on 429 with linear backoff (30s * attempt, up to 6 attempts). Future scripts pulling GDELT in bulk (Phase 1+) must budget for this — expect real wall-clock cost, not just the nominal 5s/call from the handover.
- **JSON shapes matched the handover template's assumptions exactly**: `artlist` → `{"articles": [...]}` with `seendate`/`title` keys; `timelinevolraw`/`timelinetone` → `{"timeline": [{"data": [...]}]}`. No parsing changes needed beyond the above.
- **`sourcecountry:IN` returns mixed-language results**, including Hindi-script headlines (e.g. "Tata Motors बनी देश की नंबर 2 कार कंपनी"), not just English-language Indian sources. This matters for later qualitative sampling (Phase 2 `event_samples.csv`) — some headlines will need translation context to assess "does this spike correspond to real identifiable news." Not a blocker, just a fact to carry forward.
- **Windows console encoding fix**: default Python stdout on this machine is cp1252, which cannot print the Hindi-script headlines above and crashes with `UnicodeEncodeError`. Added `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` at the top of `00_smoke_gdelt.py`. Same fix will be needed in any future script that prints raw GDELT text.

## Phase 1

- **Universe source**: full Nifty Midcap 100 and Nifty Smallcap 100 constituent lists pulled live (July 2026 vintage) via web search/fetch from smart-investing.in, since niftyindices.com's own CSV endpoints timed out / connection-reset from this network.
- **Selection process**: built a ~100-candidate pool from both indices, then narrowed to 40 by (a) dropping names issued via IPO/listing/demerger too recent to have Yahoo Finance price history back to 2022-07-01 (e.g. Groww/BillionBrains Garage Ventures, Waaree Energies, Premier Energies, Vishal Mega Mart, Lenskart, Meesho, PhysicsWallah, Urban Company, Pine Labs, Ola Electric, Jyoti CNC Automation, Sagility India, JSW Cement, Brainbees/FirstCry, Inventurus Knowledge Solutions) — actual price-history availability is re-verified mechanically in Phase 3, this was just a pre-filter to avoid wasted GDELT calls; (b) dropping names with unclear/discontinuous ticker history from recent corporate actions (GE Vernova T&D India, Piramal Finance, Cholamandalam Financial Holdings) since verifying continuity wasn't worth the time at this stage; (c) dropping single-acronym or generic-word names judged too ambiguous for string matching (ITI Ltd, UPL Ltd, SRF Ltd, Anant Raj Ltd — the last being a common personal name); (d) capping redundancy within any one sector.
- **Moderate distinctiveness risk, kept anyway**: CESC Ltd, NHPC Ltd, NMDC Ltd, Oil India Ltd, Lupin Ltd, Aarti Industries Ltd, Blue Star Ltd, The Federal Bank Ltd — each is a short/acronym/common-word name, but the GDELT query is an exact quoted phrase (full legal name incl. "Ltd") plus `sourcecountry:IN`, which should filter most collisions (e.g. "Operation Blue Star" doesn't match "Blue Star Ltd"). Flagged for a specific look during the Phase 2 `event_samples.csv` qualitative spot-check rather than excluded outright, since excluding them would have disproportionately cut power/mining/pharma sector representation.
- **Final universe**: 40 tickers across 12 broad sector buckets (pharma, IT services, private banks, NBFC/insurance, auto, FMCG, metals/mining, power, real estate, agrochemicals/fertilizers/chemicals, consumer durables, industrial engineering) — well above the ≥6 sector minimum. Written to `data/universe.csv`.
- **Coverage pull throughput is slow and noisy.** GDELT rate-limits this network hard enough that `scripts/01_coverage.py` (25s/call + retry backoff) processes roughly 6-13 ticker-years/hour, not the ~144/hour a naive 25s cadence would suggest — real throughput is dominated by 429 backoff, not the nominal sleep. Full 40-ticker (120-call) pull realistically spans many hours; script caches every ticker-year response to `data/raw/*.json` immediately on success specifically so it can be killed and resumed freely across a session boundary.
- **Crash bug found and fixed mid-run**: `call_with_retry` originally only retried on HTTP 429 and connection-level exceptions. At ticker 27/40 (SAIL) GDELT returned a 200 response with an empty/invalid body, `r.json()` raised an uncaught `JSONDecodeError`, and the whole process died (63 of 120 ticker-years already cached and preserved, no data lost, but had to be manually relaunched). Fixed by also retrying on HTTP 5xx and on JSON-decode failure of an otherwise-200 response, with the same backoff schedule. Any future non-retryable exception should be treated as a real bug to fix, not restarted past — this is exactly the kind of failure that must surface loudly per rule 3, not be silently swallowed.
- **SAIL and BHEL dropped from the universe.** Both consistently failed all fetch attempts across three separate script runs (18 total attempts each: 3 years × 6 retries), including a run where they were fetched first, in isolation, with zero preceding requests that session — ruling out "queue position absorbs accumulated rate pressure" as the cause. Root cause not conclusively identified; most likely an IP-level throttle that hardened over ~6 hours of continuous same-day querying, past what a 180s backoff ceiling can clear. Per rule 3 (fail loudly, don't fabricate, stop rather than force it) and following the same drop-and-log precedent HANDOVER.md §Phase 3 sets for broken price data, both tickers were removed from `data/universe.csv` rather than retried indefinitely. Final universe: **38 tickers**. Sector spread unaffected (still well above the ≥6 minimum) — SAIL was the only "Steel & Iron Products" entry and BHEL the only "Engineering - Industrial Equipments" entry, but 11 other sector buckets remain. Could be retried in a future session (e.g. next day, cooler rate-limit window) if desired, but not blocking — the coverage gate passes comfortably without them.
- **Coverage gate result (final, 38 tickers, all with complete 3-year 2023-2025 GDELT `timelinevolraw` data):** median nonzero-volume days/year = **104.5**, well above the ≥30 threshold. **GATE: PASS.** Full detail in `data/coverage_summary.csv`. No universe-shift interruption needed (HANDOVER §4.1's escalation path only triggers on gate *failure*).
- **Query construction**: per-ticker GDELT query is `("<company_name>" OR "<alias1>" OR ...) sourcecountry:IN` when aliases exist (built in `scripts/01_coverage.py::build_query`), else just `"<company_name>" sourcecountry:IN`. Confirmed GDELT requires OR'd terms to be parenthesized (unparenthesized OR queries return a plain-text 200 error, not JSON) — the script already does this correctly; verified by direct query during the SAIL/BHEL investigation.

## Phase 2

- **Event detection is all-or-nothing per ticker by design.** `scripts/02_events.py::load_series` requires all 3 years of a series (vol or tone) to be cached or it returns `None` for that whole ticker, and the caller skips any ticker missing either series entirely rather than computing partial-year `vol_z`/`tone_z`. This caused the first full run to silently under-count events (165, from only 19/38 tickers with complete tone data) before two more gap-fill passes brought all 38 tickers to complete 2023-2025 tone coverage and the count rose to the final **242**. Rule: never treat an event/coverage number as final while any ticker in the run log shows "SKIP" — rerun (the script is fully cache-resumable) until skip count is zero.
- **Final event count: 242 events across 28 of 38 tickers** (10 tickers had zero qualifying spike days at `vol_z>=3, v>=5` — expected, not a data gap, confirmed via zero `SKIP` lines and 114/114 tone files cached). Primary threshold used; **pre-registered fallback NOT triggered**. Gate (>=150): **PASS, confirmatory**. Direction split: 154 events tone-positive, 88 tone-negative — not degenerate.
- **Qualitative spot-check of the 20 largest spikes (`data/event_samples.csv`, up to 5 headlines each via `artlist`)**: the large majority are genuine, identifiable, company-specific news — earnings releases (PIIND, ICICIGI, COLPAL x2), a real corporate-action/deal (BHARATFORG/Rolls-Royce, MPHASIS/Blackstone block deal), an accident (COROMANDEL ammonia gas leak), a crisis story (CESC/Kolkata electrocution deaths), a major geopolitical story with direct company relevance (NHPC/Indus Waters Treaty), and a real estate sale (GODREJPROP/Noida project). 5 of 20 events returned no articles (artlist retries exhausted — a sampling gap, not evidence the event itself is spurious). **2-3 events show market-wide-roundup contamination**: LICHSGFIN's sampled headlines are a generic "buy calls vanishing from India's market" story with no company-specific content at all, M&MFIN's are a mix of one relevant analyst note and several generic "stocks to watch" round-ups, and OIL's are about IOC/GAIL/ONGC fines rather than Oil India specifically (most likely a multi-PSU roundup article that mentions Oil India in the body without featuring it in the headline). This is exactly the **cross-sectional clustering** risk HANDOVER.md §4.7 already anticipated and pre-registered a mitigation for (the >3-simultaneous-ticker-day robustness drop) — not a new problem, but this sample is the first direct evidence it's real in this dataset, not just theoretical.
- **Stray autonomous commit found and undone.** A local-only commit (`f2844ca`, message style "feat: ..." not matching this project's `phase N: ...` convention) appeared mid-Phase-2, evidently made by an autonomous background check-in that overstepped its brief (check progress and report, not commit) and captured a stale draft of `PREREGISTRATION.md`/`scripts/02_events.py` before the data was actually complete. It was never pushed, so no shared history was affected. Undone with `git reset --soft HEAD~1` (kept all file changes, just removed the premature commit) and redone properly as the real `phase 2` commit once everything was genuinely finished. Lesson: treat anything an autonomous check-in does beyond its literal instructions as unverified until inspected directly.

## Phase 3

- **Price fetch was clean on the first pass** — yfinance (a different provider than GDELT) hit no rate limiting; batch-downloaded all 38 tickers + `^NSEI` in one `yf.download()` call rather than per-ticker, which was both faster and simpler than Phase 1/2's throttled-loop pattern. All 38 tickers + benchmark passed integrity checks (full 2022-07-01 to 2026-01-31 coverage, 0 non-positive closes, 0 extreme single-day moves >50% log-return) — **no tickers dropped**, no universe regeneration needed.
- **Integrity criteria used** (`scripts/03_prices.py`): a ticker is dropped if its first trading day isn't within 21 days of 2022-07-01, its last isn't within 21 days of 2026-01-31, its row count is >5% below the `^NSEI` benchmark's row count, or it has any non-positive close price. Extreme single-day moves (|log return| > 50%) are reported per ticker in `price_integrity.csv` but don't trigger an automatic drop, since a real large move (e.g. a stock-specific shock) is a legitimate data point, not necessarily bad data — none occurred in this run regardless.
- **`missing_pct` shows small negative values (~-0.1%) for most tickers**: this just means that ticker had 1 more trading day than `^NSEI` itself over the window (a minor NSE equity-vs-index calendar difference, e.g. a day equities traded but the index had a data gap), not missing data. Cosmetic only.

## Phase 4

- **Event study ran clean**: all 242 events got a full 120-observation estimation window (market-model beta fit for every event, zero market-adjusted fallbacks needed) — consistent with Phase 3's clean, gapless price data. Only one NaN: NATIONALUM's 2025-12-31 event (the last day of the study period) lacks 20 forward trading days within the fetched price range (which ends 2026-01-30) to compute CAR_t20 — flagged in the script's own report, not silently dropped or fabricated. CAR_t1/CAR_t5 distributions are centered near zero with plausible spread (see `results/car_by_event.csv`), no signs of a computation bug.
- **Followed HANDOVER.md's own phase numbering, not ARCHITECTURE.md's** (parked, post-v0.1): `scripts/04_event_study.py` (CAR computation) and `scripts/05_inference.py` (permutation null + verdict) are committed as two separate phases (`phase 4` / `phase 5`), matching HANDOVER's original plan. ARCHITECTURE.md's "Phase 4: VALIDATION CORE" language collapses both into one label, which caused a momentary mislabeling in conversation — corrected before committing, no code impact.
- **Permutation engine uses a closed-form rolling OLS**, not a per-event `lstsq` refit, for the ~242,000 pseudo-event recomputations (1,000 draws x ~242 events). Validated to floating-point precision against `04_event_study.py`'s per-event `lstsq` output before trusting it (see script docstring). Runtime: ~35s for the full 1,000-permutation run — no rate limits or background execution needed for this phase, unlike Phases 1-3.
- **Permutation null construction**: for each ticker, real events' `tone_z`/`direction` values are paired with freshly-drawn pseudo-event entry dates (same count per ticker as real events, drawn without replacement from that ticker's trading days in the study period, excluding +/-5 trading days around each of that ticker's *real* event entry dates). This holds the marginal distribution of announced tone fixed while randomizing timing — the standard construction for testing whether the *timing* of real spikes carries information, not just their tone values in the abstract.
- **Tone-tercile spread secondary test required its own null** (not just an observed value) to fit properly into the BH-FDR correction alongside the other five secondary tests — implemented by reusing the same fixed tercile membership (based on real `tone_z`, computed once) applied to each permutation's pseudo-CAR_t5 values, rather than re-deriving tercile assignment per permutation (tone_z never changes across draws, only which dates get paired with it).
- **Robustness pass, as pre-registered (one pass, after primary/secondary already recorded)**: (a) fallback spike threshold — not applicable, primary threshold already cleared the 150-event gate with margin; (b) market-adjusted (beta=1) returns — reported for transparency (p=0.833) but explicitly *not* a like-for-like comparison against the market-model null, since the null wasn't rebuilt under market-adjusted assumptions; (c) drop calendar days with >3 simultaneous tickers — **0 days met that threshold** in this dataset (max observed simultaneity was exactly 3, confirmed directly against `events.csv`), so decluster and non-declustered results are identical.
- **VERDICT: NULL.** Primary test: `mean(sign(tone_z) x CAR_t5) = -0.00151`, one-sided permutation p=0.763 (not significant, and the point estimate is even the *wrong* sign relative to the thesis). No secondary test survives BH-FDR at q=0.10 (best secondary p-value: 0.238, for `abs_CAR_t1_vs_null`). Robustness pass doesn't change the conclusion. Per HANDOVER.md's pre-registered kill criterion (§4.8): "no detectable edge under this specification." This is a full, honest result per the pre-registration — not a failure to fix, and no respecification is permitted now that results have been seen. Full detail: `results/stats.json`.

## Phase 6

- **Raw null distributions persisted retroactively**: `scripts/05_inference.py` originally only wrote summary p-values to `stats.json`, not the 1,000 raw draws per statistic. Added a `results/null_distributions.csv` export and re-ran (same seed=42, so `stats.json` is byte-for-byte reproduced — confirmed via `git diff`) so Phase 6's figures could plot the actual null distributions rather than approximating them from summary stats.
- **Chart design**: invoked the project's dataviz skill for form/color guidance, adapted to this project's static-PNG-only constraint (matplotlib, per HANDOVER's allowed stack — no interactive HTML/JS, which the skill defaults to). Used the skill's validated diverging palette (blue `#2a78d6` / red `#e34948`, gray midpoint) for polarity encodings (observed-vs-null, tone tercile sign) and the same blue for magnitude-only bars, rather than inventing colors. Three figures: primary null histogram with observed value marked, tone-signed CAR observed-vs-null-interval across all three horizons, and CAR_t5 by tone tercile (the clearest single visual of the "wrong sign" finding — bottom/negative-tone tercile has the highest mean CAR_t5).

## Phase 5 (post-v0.1, "override handover" — see PARKING.md)

- **`11_daily_events.py` bug caught before commit**: the first-run fallback for "no previous events file yet" built an empty `DataFrame(columns=["ticker", "event_date"])` and then called `.dt.date` on the `event_date` column — but an empty column with no data has no dtype to be datetime-like, so `.dt` raised `AttributeError`. Fixed by skipping the `.dt` access entirely when no previous file exists (`previous_keys = set()`) instead of routing an empty DataFrame through the same code path as a populated one.
- **First live gap-fill was ~6 months, not 1 day**: historical study data ends 2025-12-31; "yesterday" relative to the actual current date (2026-07-09) is mid-2026, so the very first `10_daily_ingest.py` run had a real ~6-month per-ticker gap to fetch, hitting the same GDELT throttling as the original historical pulls. Took 3 passes (some tickers timed out/429'd and needed retrying, same self-healing pattern as Phases 1-2) to fully close. This is expected and one-time — every future scheduled run is a true 1-day top-up.
- **Live event count (275 across 29 tickers) differs from v0.1's locked study (242 across 28 tickers)** — expected, not a discrepancy: the live store's date range extends through 2026-07-08, six months past the locked study's 2025-12-31 cutoff, so more calendar time naturally produces more qualifying spike days. `data/live/events.csv` and `data/events.csv` are deliberately separate and neither overwrites the other.
- **Idempotency verified directly**: reran both `10_daily_ingest.py` and `11_daily_events.py` back-to-back with no gap remaining — GDELT fetch correctly reports "nothing new for any ticker" for both series, and the events diff correctly reports "New since last run: 0". The price fetch always re-requests its trailing lookback window (to catch corrections) but nets zero new rows when nothing changed, which is the intended behavior, not a bug.
- **`daily.yml` verified end-to-end via manual `workflow_dispatch`** (run [29013080177](https://github.com/vivektmurali/obscura-intel/actions/runs/29013080177), triggered via `gh workflow run`): checkout, dependency install, both scripts, and the git commit/push step all ran successfully on Actions' actual ubuntu-latest runner (not just locally). Since local data was already fully caught up before this run, it correctly exercised the "nothing changed" path — GDELT reported no gap, events diff reported 0 new, and `git commit` correctly no-op'd ("nothing to commit, working tree clean") rather than creating an empty commit. ARCHITECTURE.md's Phase 5 gate ("3 consecutive unattended green runs with real appended data") will complete itself as the 01:15 UTC cron fires on subsequent days and finds genuine new data to append — nothing further to do manually.

## Phase 6 (post-v0.1, same override — NULL-mode scoring only)

- **Verdict-branch guard is a hard `sys.exit`, not a comment.** `scripts/06_scoring.py` reads `results/stats.json`'s verdict at runtime and refuses to run (exit 1) if it's ever anything other than `NULL` — SIGNAL-mode scoring (historical CAR(t+5) bucket stats) isn't implemented, and silently falling through to NULL-mode logic on a future SIGNAL verdict would violate the claims-audit gate by under-reporting what validation actually supports. If the verdict ever changes, this script needs deliberate extension, not automatic reinterpretation.
- **Claims-audit gate enforced by assertion, not convention**: `score_null_mode` asserts no `CAR_t*` column is present in the frame before scoring. NULL mode must never show or imply forward returns per ARCHITECTURE.md §7 — this makes that a code-level guarantee rather than something that could quietly regress.
- **Intensity percentile uses v0.1's locked historical `vol_z` distribution as the reference** (`data/events.csv`, 242 events, audited and fixed), not the ever-growing live event table. Reasoning: percentiles against a moving reference would drift in a confusing way (the same event's "intensity" changing over time as more live events accumulate) — anchoring to the frozen, already-validated distribution keeps the score's meaning stable.
- **Novelty is per-ticker days-since-last-event within the live cumulative table** (`data/live/events.csv`), `NaN` for a ticker's first-ever recorded event (no prior event to diff against) rather than 0 or some other placeholder — an explicit "not applicable" beats a fabricated number.
- **Optional LLM enrichment (ARCHITECTURE.md §8, Phase 6 item 3) is explicitly not built.** It's a separately-banned item (Claude API calls in the pipeline) needing its own override before any of it — the earn-its-place historical backtest, the taxonomy classifier, all of it — gets touched.
- **Updated `daily.yml` (with scoring step) re-verified via a second manual `workflow_dispatch`** (run [29013524905](https://github.com/vivektmurali/obscura-intel/actions/runs/29013524905)): scoring step correctly read the verdict (NULL) and ran on Actions infrastructure exactly as it did locally.

## Phase 7 (post-v0.1, separate "override handover" — public site)

- **Implementation choice: Jinja2 + static HTML, not React** (ARCHITECTURE.md's option A, not B). Reasoning: this project's stack is Python-only end to end; a React static build would add a whole separate Node/npm toolchain (install, build step, lockfile) purely for a site with no interactivity requirements beyond navigation and static charts. Jinja2 keeps the "boring stack" principle intact and lets `scripts/12_build_site.py` sit alongside the rest of the pipeline with zero new tooling categories — only one new Python dependency (`jinja2`, added to `requirements.txt`).
- **GitHub Pages hosting: classic branch-based (`main` /docs), not Actions-based deployment.** Simpler — no separate Pages environment/permissions/deploy-pages action needed, just a committed `docs/` folder GitHub serves directly. Enabled via `gh api repos/.../pages -X POST`. Deviates from ARCHITECTURE.md's suggested `site/` path since GitHub's branch-based Pages only serves from repo root or `/docs` — logged here rather than silently renamed.
- **Charts are matplotlib PNGs embedded per ticker page**, not a JS charting library — consistent with the "no new tooling" reasoning above and with how v0.1's report figures were built. Event days are overlaid as thin vertical lines (green = positive tone, red = negative tone) directly on the price line.
- **Design treatment**: since this is portfolio-facing and meant to be shared (not a throwaway status page), gave it an editorial treatment rather than a generic dashboard template — invoked the artifact-design skill for the process even though the output isn't a claude.ai Artifact (the design thinking transfers; the CSP-driven "inline fonts as data URIs" advice doesn't apply to a real externally-hosted site, so fonts are loaded via a normal Google Fonts `<link>`). Typography: "Newsreader" (serif, editorial) for headings, "IBM Plex Sans" for body, "IBM Plex Mono" for tickers/data — deliberately avoiding Inter/Space Grotesk (flagged as the generic "safe" AI-generated look). Color continues the amber/ink identity already established in the v0.1 report figures and the private status artifact, refined into a full token system (paper/ink/accent/good/bad, light+dark via `prefers-color-scheme`).
- **Framing choice for the hero copy**: since the verdict is NULL, the homepage explicitly frames the live feed as "an instrument that watches constantly and, so far, correctly reports nothing" rather than downplaying or omitting the null result — consistent with the project's "honesty over momentum" rule and avoids the site accidentally overclaiming what a quiet daily cron actually demonstrates.
- **Live feed shows intensity + tone direction only, never a forward-return number** — same claims-audit constraint as `06_scoring.py`, restated in the page copy itself so a reader doesn't need to infer it.
- **`docs/` is committed to git** (required for classic Pages hosting to serve it) and rebuilt from scratch (`shutil.rmtree` + regenerate) on every run rather than incrementally patched — matches the project's "recompute over append" principle and avoids stale pages lingering after a ticker/event is removed or changed.

## Phase 8 (post-v0.1, same override — hardening)

- **Freshness guard implemented as a hard build failure, not a banner**: `scripts/12_build_site.py` now checks `data/live/prices.parquet`'s max date against today (UTC) and `sys.exit(1)`s if it's more than `MAX_STALENESS_DAYS` (4, not ARCHITECTURE's literal 3) calendar days behind. Used 4 instead of 3 specifically to avoid a routine Friday-close-to-Monday-build gap over a weekend tripping the guard on every single week — 3 calendar days is exactly a normal weekend gap, not an outage. A build failure here means the commit/push step after it in `daily.yml` never runs, so Pages keeps serving the last known-good build rather than a page that looks current but isn't. Footer now shows "data current through {date}" separately from "built {time}", so a reader can tell the two apart if they ever diverge.
- **Data growth policy: documented, not implemented.** Current live store is ~2.5MB (`data/live/`), growing by roughly 115 rows/day across three parquet files — at that rate, reaching ARCHITECTURE.md's 500MB compaction threshold would take on the order of decades. Building monthly-partition compaction or a separate data branch now would be premature engineering against a threshold nowhere close to being hit. Revisit if `du -sh data/live/` ever approaches the low hundreds of MB.
- **NSE bhavcopy price-source fallback: deliberately not implemented.** Couldn't confirm the current, correct download URL/format with confidence — NSE changed the bhavcopy format in 2024 (per public reports), and a direct fetch attempt against `nseindia.com` timed out, consistent with the site's known aggressive bot protection (session cookies from a prior homepage visit are typically required before their data endpoints respond). Building this half-verified would mean one of two bad outcomes: a fallback that's silently broken (false confidence, exactly the failure mode this project exists to avoid), or scraping-evasion behavior (spoofed headers, cookie-jar warm-up) that edges uncomfortably close to HANDOVER.md §5's "no scraping beyond the GDELT API" ban even though bhavcopy itself is a data file, not HTML scraping. yfinance has had zero failures across every run this entire project (Phases 3, 5-8) — the practical case for this fallback is currently thin. Flagging as deferred rather than building a low-confidence stub.
- **`daily.yml` with all Phase 8 additions verified via a third manual `workflow_dispatch`** (run [29027636821](https://github.com/vivektmurali/obscura-intel/actions/runs/29027636821)): full run succeeded, and the alert step correctly detected the absent `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` secrets and no-op'd cleanly rather than erroring, confirming the "alerting's absence must never fail the pipeline" requirement holds on real Actions infrastructure, not just locally.
- **v1.0 tag: not cut yet.** ARCHITECTURE.md §1's own "shipped" gate requires (a) 14 consecutive unattended daily cron runs — the cron only started today, so this needs roughly two weeks to accumulate honestly, not something to fake by backdating or batch-triggering; (b) the repo sent to >=3 real humans, at least one technical — that's Vivek's outreach to do, not something achievable from inside the pipeline. All infrastructure and code for Phases 5-8 is built, tested, and live as of this commit; tagging `v1.0` before those two boxes are genuinely checked would violate the same "phase not done until the gate passes" discipline this whole project has followed since Phase 0.

## Post-Phase-8 bug: `parse_timeline` broke on the first true 1-day gap (2026-07-11)

- **Cron run [29139679607](https://github.com/vivektmurali/obscura-intel/actions/runs/29139679607) failed** (`daily-pipeline`, 2026-07-11 04:29 UTC scheduled run, "All jobs have failed") — the first cron run to hit a genuine 1-day incremental gap rather than the multi-month backfill Phases 5-8 were built and tested against. This resets the 14-consecutive-clean-run counter toward `v1.0` back to zero (1 clean run — 2026-07-10 — before this).
- **Root cause, confirmed empirically against the live API (not guessed):** GDELT's `timelinevolraw`/`timelinetone` timeline modes auto-select bucket resolution by query span. Multi-year historical queries return exactly one daily bucket per day (`T000000Z` only — verified against the cached `data/raw/*.json` files). Short-span queries — like the live pipeline's real 1-day gaps — return 15-minute buckets (`T000000Z`, `T001500Z`, `T003000Z`, ... confirmed via a direct API call for a 2-day window). `scripts/10_daily_ingest.py::parse_timeline` hardcoded a midnight-only `"T000000Z"` strip-and-parse, so the first non-midnight bucket timestamp crashed it with an unhandled `ValueError`. The crash happened mid-fetch, before any `to_parquet` write, so no bad data was written — the pipeline's "fail loudly, don't corrupt" design worked as intended, it just needed the actual fix.
- **Fix**: `parse_timeline(data, kind)` now parses the full timestamp (`%Y%m%dT%H%M%SZ`) instead of assuming midnight, then groups by calendar date and aggregates: `sum` for `kind="vol"` (raw article counts — summing sub-daily bucket counts recovers the true daily total), `mean` for `kind="tone"` (unweighted average of per-bucket averages — an approximation, since GDELT doesn't expose per-bucket article counts to weight by; exact when buckets are already daily, i.e. the historical bootstrap path). Both call sites (`bootstrap_gdelt_store`, `fetch_gap`) now pass `kind` through.
- **Verified by rerunning the real gap locally**, not just unit logic: `10_daily_ingest.py` completed with exit 0 against the actual 2026-07-10 gap (no parse crash), followed by `11_daily_events.py` → `06_scoring.py` → `12_build_site.py`, all clean. GDELT rate-limited heavily this run (only 8/38 vol and 10/38 tone tickers got through before retries exhausted) — expected and tolerated by design; unfetched tickers simply keep their gap open and get retried on the next run (recompute-over-append self-healing), same as every prior throttling episode in this project (Phases 1, 5).
